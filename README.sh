
### Deploy

To deploy, run rake_post.sh or

`$> rake generate`
`$> rake deploy`

### Backup

To push the entire blog framework to github repo:

`$> git add -A`
`$> git commit -m 'update'`
`$> git push source source`
